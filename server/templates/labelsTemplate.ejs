<!DOCTYPE html>
<html>

<head>
    <style>
        body {
            font-family: sans-serif;
            margin: 0;
            padding: 0;
        }

        /* --- THERMAL FORMAT (80mm) --- */
        .container.thermal {
            width: 76mm;
            /* Margen seguridad */
            overflow: hidden;
            padding: 2mm;
        }

        .container.thermal .header {
            text-align: center;
            border-bottom: 2px dashed #000;
            padding-bottom: 5px;
            margin-bottom: 5px;
        }

        .container.thermal .folio-big {
            font-size: 24pt;
            font-weight: 900;
            display: block;
        }

        .container.thermal .client-info {
            font-size: 12pt;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .container.thermal .details {
            font-size: 10pt;
            margin-bottom: 5px;
        }

        .container.thermal .big-date {
            font-size: 14pt;
            font-weight: bold;
            text-align: center;
            border-top: 2px solid #000;
            border-bottom: 2px solid #000;
            margin: 5px 0;
            padding: 2px;
        }

        .container.thermal .qr-section {
            text-align: center;
            margin-top: 10px;
        }

        .container.thermal .qr-img {
            width: 40mm;
            height: 40mm;
        }

        /* --- A4 / STANDARD FORMAT --- */
        .container.a4 {
            width: 100%;
            border: 1px solid #ddd;
            padding: 20px;
            box-sizing: border-box;
        }

        .container.a4 .folio-big {
            font-size: 40pt;
            color: #ec4899;
            font-weight: bold;
        }

        .container.a4 .client-info {
            font-size: 18pt;
            margin-top: 10px;
        }

        .container.a4 .big-date {
            font-size: 20pt;
            background: #fdf2f8;
            padding: 10px;
            margin: 20px 0;
            border-left: 5px solid #ec4899;
        }

        .container.a4 .qr-img {
            width: 150px;
            height: 150px;
        }
    </style>
</head>

<body>
    <% folios.forEach(folio=> { %>
        <div class="container <%= format %>">
            <div class="header">
                <span class="folio-big">
                    <%= folio.folioNumber %>
                </span>
                <div class="client-info">
                    <%= folio.clientName %>
                </div>
            </div>

            <div class="big-date">
                ENTREGA: <%= folio.deliveryDate %> - <%= folio.deliveryTime %>
            </div>

            <div class="details">
                <strong>Especificaciones:</strong><br>
                <%= folio.persons %> px | <%= folio.shape %>
                        <% if(folio.hasExtraHeight) { %> | ⬆️ Altura Extra <% } %>
                                <br><br>
                                <strong>Descripción:</strong><br>
                                <%= folio.description %>
            </div>

            <div class="qr-section" style="position: relative; display: inline-block;">
                <img src="<%= folio.qrCode %>" class="qr-img" />
                <!-- Branded Center Logo -->
                <img src="<%= folio.logoUrl %>"
                    style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 20%; height: 20%; pointer-events: none;" />
            </div>
        </div>
        <!-- Page Break if multiple -->
        <div style="page-break-after: always; height: 1px;"></div>
        <% }) %>
</body>

</html>